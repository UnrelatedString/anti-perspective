import cv2

class Tracker:
    def __init__(self):
        self.cam = cv2.VideoCapture(0)
        cam.grab() # takes a frame or two for a good result
        cam.grab()
    def __call__(self):
        pass
